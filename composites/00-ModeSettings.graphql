# User is the controller of this model
## This is for reference only - your script will establish model access using an existing deployed composite
type ModeSetting @createModel(
    accountRelation: SINGLE
    description: "A mode settings primitive that allows users to toggle between light and dark"
  ) {
    author: DID! @documentAccount
    mode: Mode!
}

enum Mode {
  LIGHT 
  DARK 
}